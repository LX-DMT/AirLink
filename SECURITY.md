# Security policy

Do not report Wi-Fi credentials, private keys or device-identifying logs in a
public issue. Use the repository owner's private contact channel for security
reports.

The provisioning hotspot intentionally uses the fixed password `12345678`.
The Release profile removes SSH, Telnet and adbd. VirtualHere TCP port 7575 is
available during wireless sharing.
